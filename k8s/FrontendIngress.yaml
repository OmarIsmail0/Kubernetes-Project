apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: product-localhost
  namespace: default # Change if needed
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: product.localdev.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: front-end
                port:
                  number: 80
